trigger OffsetTrigger on Offset__c (after insert, after update, after delete, after undelete) {
    // Currently no Apex logic is needed.
    // This is a placeholder in case you want to add Project updates later.
}
